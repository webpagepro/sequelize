/*

SELECT notes
                       FROM asset_to_notes
                       WHERE asset_id = ?

                      UPDATE tags.asset_to_notes
               SET notes = ? WHERE asset_id = ? */